第一周（java)
计算机2104刘小靖
1. 安装了VM VirtualBox，Linux下安装了jdk8并配置好环境变量。（熟练掌握了安装过程,因为中间安
装两次出错第一次因为VMware Workstation Pro没法正常运行，一点开虚拟机就蓝屏，这个确实
不知道原因，pz学长有同样的问题，都不会。第二次是Box配置jdk路径的时候命令行出错把PATH
改错了，所有的命令行都没办法运行，只能重装)
2. 了解Linux上的基本命令行操作：q(退出），q!(强制退出)，wq(保存并退出)，sudo(以管理员身份
运行)。
3. 目录结构了解尝试了root,sbin,usr,/usr/bin(因为Box配置路径出错后一直尝试用sudo之外的其他方
法获取管理员权限发现PATH修改后所有的命令行都无法运行，所以千万不要随便修改PATH)
4. 尝试用markdown写周报（就是这个🤦‍♀️）
5. 下周继续学习markdown和目录结构
